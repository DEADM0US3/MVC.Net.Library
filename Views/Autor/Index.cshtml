@model List<FernandoLibrary.Persistance.Entities.Autor>

@{
ViewData["Title"] = "Autores";
Layout = "_Layout";
}

<div class="container mt-4">
    <h1 class="display-4">Autores</h1>

    <form asp-action="Index" method="get" class="form-inline mb-4">
        <div class="input-group">
            <input type="text" name="searchString" value="@ViewData["SearchString"]" class="form-control" placeholder="Buscar por nombre" />
            <div class="input-group-append">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-search"></i> Buscar
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="fas fa-sync-alt"></i> Limpiar
                </a>
            </div>
        </div>
    </form>

    <div class="mb-4">
        <a href="@Url.Action("Create", "Autor")" class="btn btn-success">
            <i class="fas fa-plus"></i> Agregar Autor
        </a>
    </div>

    <!-- Tabla de autores -->
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var autor in Model)
            {
            <tr>
                <td>@autor.Id</td>
                <td>@autor.Nombre</td>
                <td>
                    <a href="@Url.Action("Edit", "Autor", new { id = autor.Id })" class="btn btn-primary btn-sm">
                        <i class="fas fa-edit"></i> Editar
                    </a>
                    <a href="@Url.Action("Delete", "Autor", new { id = autor.Id })" class="btn btn-danger btn-sm">
                        <i class="fas fa-trash"></i> Eliminar
                    </a>
                </td>
            </tr>
            }
            </tbody>
        </table>
    </div>
</div>

@section Scripts {
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
}